# wedding-website-backend

# build docker images
docker-compose build

# run
docker-compose up -d

# stop
docker-compose stop

# log
docker-compose logs

# delete all volume
docker-compose down --volumes